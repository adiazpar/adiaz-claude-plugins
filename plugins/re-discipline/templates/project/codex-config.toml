# re-discipline:codex-memory-policy v1
# New re-discipline projects use shared-only memory. Preserve unrelated keys
# when merging this policy into an existing project configuration.
[features]
memories = false

[memories]
generate_memories = false
use_memories = false
# re-discipline:codex-memory-policy:end
