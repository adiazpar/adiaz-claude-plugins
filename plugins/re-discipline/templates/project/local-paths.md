# Machine-Local Paths

This untracked file is the single machine-local path map for every manager
host. Keep portable variable names and contracts in the canonical project
profile or durable project documentation. Keep only machine-local values and
local-only explanatory notes here. Load only the assignments required by the
current task; never import this whole file into manager context.

```powershell
{{LOCAL_PATH_ASSIGNMENTS}}
```
