# Directory skeleton created by init-project (one dir per line, repo-relative).
# Each is the home of one epistemic status (CLAUDE.md s1). Empty dirs get a .gitkeep.
docs
docs/truth
docs/history
docs/history/chronicles
docs/backlog
active
archive
src
tests
# tools/ is NOT force-created -- a project gets it only if it has its own code tooling.
# The agent team lives in top-level agents/ (created by greenfield Step 5 when the agent
# framework is adopted), NOT under tools/.
