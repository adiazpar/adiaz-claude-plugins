# Directory skeleton created by init-project (one dir per line, repo-relative).
# Create empty directories locally. Add .gitkeep only when repository policy
# requires it, and never inside the provider or recruiting state roots.
.re-discipline
.re-discipline/agents
.re-discipline/agents/providers
.re-discipline/agents/recruiting
.claude
.codex
# `delegate` renders drafter-AGENTS-override.md inside each subagent workspace.
docs
docs/truth
docs/history
docs/history/chronicles
docs/backlog
active
archive
src
tests
# tools/ is NOT force-created -- a project gets it only if it has its own code tooling.
# The external-provider framework always lives under .re-discipline/agents/.
