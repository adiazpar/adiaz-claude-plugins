# Recruiting Run Override

This is an isolated candidate evaluation run. Read the canonical project
profile, external drafter contract, `run.json`, and `brief.md`. Verify the
immutable context pack against the retained digest before use.

Write only this run's `report.md` and lazy `payload/`, plus exact granted paths.
Never read a manager answer key, change live provider configuration, claim
manager authority, or edit canonical project state.
