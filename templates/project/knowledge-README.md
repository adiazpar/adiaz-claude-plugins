# Knowledge System

This directory is machine-managed. The knowledge search system configures,
measures, and repairs itself here; nothing in it needs hand-editing.

To change how it behaves - what gets indexed, how much context it returns,
memory mode - just ask the agent in plain language.
